import { toHtmlClass } from 'pac-man/helpers/to-html-class';
import { module, test } from 'qunit';

module('Unit | Helper | to html class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toHtmlClass([42]);
  assert.ok(result);
});
